const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
export function adminPage(rows=[],loading=false){return `<div class="page-head"><div><span class="eyebrow">ACESSO ADMINISTRATIVO</span><h1>Casos clínicos enviados</h1><p>Acesso restrito, auditável e autorizado por RLS. Use somente para a finalidade institucional informada.</p></div></div>${loading?'<section class="panel">Carregando…</section>':`<section class="panel"><div class="section-title"><h2>Pacientes sincronizados</h2><span class="group-count">${rows.length}</span></div><div class="admin-case-grid">${rows.map(row=>`<article class="reference-card"><small>${esc(row.updated_at||'')}</small><h3>${esc(row.patient_code||'Paciente')}</h3><p>Conta: ${esc(row.owner_id)}</p><details><summary>Ver dados clínicos</summary><pre>${esc(JSON.stringify(row.snapshot,null,2))}</pre></details></article>`).join('')||'<p>Nenhum caso sincronizado.</p>'}</div></section>`}`;}
